package pagination_test

import "testing"

func TestFromEchoContext(t *testing.T) {
	t.Log("No test to to run in TestFromEchoContext")
	for _, tc := range []struct {
		name string
	}{} {
		tc := tc
		t.Run(tc.name, func(t *testing.T) {
			t.Parallel()
			//TODO: Add test
		})
	}
}
